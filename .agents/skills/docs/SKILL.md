---
name: docs
description: Use when editing README, example-Agent, docs, project-map docs, skills, or agent-facing instructions.
---

# Docs

- Keep docs aligned with the current GutConference CMS product, not the source project.
- Default local server port is `6040`.
- If a product workflow changes, update `AGENTS.md`, `example-Agent.md`, and all mirrored skill files under `.codex/skills/`, `.claude/skills/`, and `.agents/skills/`.
